{
  "name": "rexauth",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "npm run server --prefix backend",
    "client": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "author": "Tarek Mouchmouch",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
